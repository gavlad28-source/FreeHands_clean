<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Small components (buttons, chips, etc.) -->
    <style name="ShapeAppearance.FreeHands.SmallComponent" parent="ShapeAppearance.MaterialComponents.SmallComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">4dp</item>
    </style>

    <!-- Medium components (cards, dialogs, etc.) -->
    <style name="ShapeAppearance.FreeHands.MediumComponent" parent="ShapeAppearance.MaterialComponents.MediumComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">8dp</item>
    </style>

    <!-- Large components (bottom sheets, modals, etc.) -->
    <style name="ShapeAppearance.FreeHands.LargeComponent" parent="ShapeAppearance.MaterialComponents.LargeComponent">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>
</resources>
