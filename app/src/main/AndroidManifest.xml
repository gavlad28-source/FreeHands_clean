<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Required for voice recognition -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- System control permissions -->
    <uses-permission android:name="android.permission.BLUETOOTH" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WRITE_SETTINGS" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.ACCESS_NOTIFICATION_POLICY" />

    <!-- For phone and SMS commands with confirmation -->
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <!-- Query installed apps -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28"
        tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <!-- Biometric authentication -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT"
        android:required="false" />

    <!-- Device admin for advanced controls -->
    <uses-permission android:name="android.permission.BIND_DEVICE_ADMIN" />

    <!-- Accessibility service for better integration -->
    <uses-permission android:name="android.permission.BIND_ACCESSIBILITY_SERVICE"
        tools:ignore="ProtectedPermissions" />

    <!-- Required features -->
    <uses-feature
        android:name="android.hardware.microphone"
        android:required="true" />

    <uses-feature
        android:name="android.software.voice_recognizers"
        android:required="true" />

    <application
        android:name=".FreeHandsApp"
        android:allowBackup="false"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.FreeHands"
        android:usesCleartextTraffic="false"
        android:requestLegacyExternalStorage="false"
        android:fullBackupContent="false"
        android:dataExtractionRules="@xml/data_extraction_rules"
        tools:targetApi="31">
        
        <!-- Main Voice Command Receiver -->
        <receiver
            android:name=".receiver.VoiceCommandReceiver"
            android:enabled="true"
            android:exported="true">
            <intent-filter>
                <action android:name="com.freehands.assistant.action.VOICE_COMMAND" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        
        <!-- Voice Command Service -->
        <service
            android:name=".service.VoiceCommandService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/voice_command_service"
            android:permission="android.permission.BIND_VOICE_INTERACTION" />
            
        <!-- Biometric Authentication Activity -->
        <activity
            android:name=".ui.auth.BiometricAuthActivity"
            android:exported="false"
            android:theme="@style/Theme.FreeHands.Transparent" />
            
        <!-- Command Confirmation Dialog Activity -->
        <activity
            android:name=".ui.dialog.CommandConfirmationActivity"
            android:exported="false"
            android:theme="@style/Theme.FreeHands.Dialog"
            android:windowSoftInputMode="adjustResize" />
            
        <!-- Settings Activity -->
        <activity
            android:name=".ui.settings.SettingsActivity"
            android:exported="false"
            android:label="@string/title_activity_settings"
            android:parentActivityName=".presentation.MainActivity">
            <meta-data
                android:name="android.support.PARENT_ACTIVITY"
                android:value=".presentation.MainActivity" />
        </activity>
        
        <!-- Main Activity -->
        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.FreeHands.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        
        <activity
            android:name=".ui.audio.AudioTestActivity"
            android:exported="false"
            android:label="Audio Feature Test"
            android:theme="@style/Theme.FreeHands">
        </activity>
        
        <!-- Voice Assistant Service -->
        <service
            android:name=".data.service.VoiceAssistantService"
            android:enabled="true"
            android:exported="false"
            android:foregroundServiceType="microphone"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/voice_assistant_service"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:stopWithTask="false">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
                <action android:name="com.freehands.assistant.action.START_VOICE_ASSISTANT" />
            </intent-filter>
        </service>
        
        <!-- Boot Completed Receiver -->
        <receiver
            android:name=".receiver.BootCompletedReceiver"
            android:enabled="true"
            android:exported="true"
            android:directBootAware="true"
            android:permission="android.permission.RECEIVE_BOOT_COMPLETED">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.LOCKED_BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
                <action android:name="com.htc.intent.action.QUICKBOOT_POWERON" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </receiver>
        
        <!-- Custom Speech Recognition Service -->
        <service
            android:name=".service.CustomSpeechRecognitionService"
            android:enabled="true"
            android:exported="false"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:process=":voiceinteractor">
            <intent-filter>
                <action android:name="android.speech.RecognitionService" />
            </intent-filter>
            <meta-data
                android:name="android.speech.RecognitionService"
                android:resource="@xml/recognition_service" />
        </service>
        
        <!-- Voice Interaction Service (for Android 6.0+ devices) -->
        <service
            android:name=".service.FreeHandsVoiceInteractionService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:process=":voiceinteractor"
            android:label="@string/voice_interaction_service"
            android:icon="@mipmap/ic_launcher">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction"
                android:resource="@xml/voice_interaction_service" />
            <meta-data
                android:name="android.voice_interaction_ui"
                android:resource="@xml/voice_interaction_ui" />
        </service>
        
        <!-- Voice Interaction Session Service -->
        <service
            android:name=".service.VoiceInteractionSessionService"
            android:enabled="true"
            android:exported="true"
            android:permission="android.permission.BIND_VOICE_INTERACTION"
            android:process=":voiceinteractor">
            <intent-filter>
                <action android:name="android.service.voice.VoiceInteractionService" />
            </intent-filter>
            <meta-data
                android:name="android.voice_interaction_session_service"
                android:resource="@xml/voice_interaction_session_service" />
            <meta-data
                android:name="android.voice_interaction_ui"
                android:resource="@xml/voice_interaction_ui" />
        </service>
    </application>

</manifest>
