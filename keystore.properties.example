# Keystore Configuration Example
# Copy this file to keystore.properties and fill in your actual values
# NEVER commit keystore.properties to version control!

# Path to your keystore file (relative to project root or absolute path)
storeFile=freehands-release.keystore

# Password for the keystore
storePassword=YOUR_KEYSTORE_PASSWORD_HERE

# Alias of the key to use
keyAlias=freehands-release-key

# Password for the key
keyPassword=YOUR_KEY_PASSWORD_HERE

# Instructions:
# 1. Generate a keystore using:
#    keytool -genkey -v -keystore freehands-release.keystore -alias freehands-release-key -keyalg RSA -keysize 2048 -validity 10000
#
# 2. Copy this file to keystore.properties
#
# 3. Update the values above with your actual keystore information
#
# 4. Add keystore.properties to .gitignore to prevent committing secrets
